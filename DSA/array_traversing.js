let sampleArray = [ 1,2,3,4,5,8] ;

// going through or read the each element of data is traversing ;
for(let i=0 ; i < sampleArray.length ; i++){
    console.log("i-",i, "value is" , sampleArray[i]);
}